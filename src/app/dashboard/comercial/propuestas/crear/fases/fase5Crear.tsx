import React, { FC } from 'react';

interface Fase5CrearProps {
      setFaseCreacionPropuesta: (fase: number) => void;
}

const Fase5Crear: FC<Fase5CrearProps> = ({setFaseCreacionPropuesta}) => {
  return (
    <div>Fase5Crear</div>
  );
};

export default Fase5Crear;