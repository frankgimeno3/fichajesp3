import React, { FC } from 'react';

interface CrearEdicionProps {
  
}

const CrearEdicion: FC<CrearEdicionProps> = ({ }) => {
  return (
    <div>CrearEdicion</div>
  );
};

export default CrearEdicion;