import React, { FC } from 'react';

interface CrearClienteProps {
  
}

const CrearCliente: FC<CrearClienteProps> = ({ }) => {
  return (
    <div>CrearCliente</div>
  );
};

export default CrearCliente;