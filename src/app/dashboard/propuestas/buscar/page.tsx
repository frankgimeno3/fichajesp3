import React, { FC } from 'react';

interface BuscarPropuestasProps {
  
}

const BuscarPropuestas: FC<BuscarPropuestasProps> = ({ }) => {
  return (
    <div>BuscarPropuestas</div>
  );
};

export default BuscarPropuestas;